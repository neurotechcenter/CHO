the way to concat the parts of ECOG and SEEG datasets

To recombine the split files: 
If you want to merge the split files back into a single archive, use the following command in the bash:

cat part_of_zs_ecog_dataset_zip_file_* > ze1-6_ecog_resting_data.zip

cat part_of_zs_seeg_dataset_zip_file_* > zs1-6_seeg_resting_data.zip
